import React from 'react';
import './styles.scss';
import EmailPassword from '../../components/EmailPassword';

const Recovery = () => {
    return (
       <EmailPassword />
    );
};

export default Recovery;
